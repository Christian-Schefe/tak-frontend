import { TimeControlPipe } from './time-control-pipe';

describe('TimeControlPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeControlPipe();
    expect(pipe).toBeTruthy();
  });
});
